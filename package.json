{
  "name": "social-media-link-preview-monorepo",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "dependencies": {},
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build && node ./build.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "lerna": "^8.1.9"
  }
}
